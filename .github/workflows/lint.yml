name: Lint
on:
  pull_request:
    branches: [dev]
  push:
    branches: [dev]
jobs:
  lint:
    runs-on: [self-hosted, rbcn]
    steps:
      - uses: actions/checkout@v4
      - name: Basic checks
        run: |
          # Repo-specific lint goes here.
          # Reuse rbcn-github-actions/.github/workflows/* once available.
          echo "placeholder lint"
